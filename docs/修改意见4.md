0.之前我提出的 (一些输入框中的"默认"/"自动分配"/"自动"), 我的意思是可以参照 "UDP单播"中的"对端地址"的输入框一样, 输入框中看着就像有字, 但是实际上根本就不是输入, 鼠标放到"对端地址"的输入框中, 输入位置会自动在第一个位置, 输入一个字后背景提示就消失, 而下方的"对端端口", 看着虽然也是淡淡的"自动"两个字, 但是鼠标放到输入框中, 输入的位置是接着"自动"两个汉字后面, 也就是汉字也变成了输入, 不能直接输入端口号数字, 得必须手动选择左右输入再次输入数字才能替换刚刚的汉字最后才能算合法输入, 所以我的意思是这些输入框中默认就是空白的而不是有汉字输入, 空白输入就表示默认而不是"自动"汉字输入表示默认, 没有输入时有一个hint提示
1."文件"中的"限速"开关,"追加写入"开关,"TCP客户端"中的"本机IP:PORT"自动分配开关, 以及"数据预览"中的日志"记录"开关, 能不能做成不可用(用户不可操作)的时候颜色变淡变灰?
2.用户点击"开始转发"如果成功了, "停止转发"的红色按钮就会激活常亮可以操作, 建议此时把"开始转发"按钮弄成禁用并灰色, 就是这两个按钮同时只有一个是常亮的可以操作的
3.串口波特率的下载菜单稍微做宽一点, 波特率显示不全, "波特率"/"数据位"/"校验"/"停止位"/"流控" 这一行每个空间都向下单独成为一行, 显示的宽度样式和上方的串口列表一致
4.数据源中下方的"已打开"/"未打开"这个文字单独占了一行, 把他删除了, 不需要, 上面的数据源"打开"/"关闭"按钮就能显示开启状态了
5.数据源中的下方的"速率"也单独占用了一行, 把它移动到 "数据预览" 中的日志 "记录"/"格式"/"时间戳" 这一行后面 (这一行后面还有很多空白空间), 让他们在一行显示
6.把"开始转发"/"停止转发"/"记录日志"/"basedir输入框"/"浏览按钮"这一行中的 "浏览按钮" 移动到 "停止转发" 按钮后面, 样式和"开始转发"/"停止转发"保持一致, 颜色改成绿色, 并且做好互斥逻辑, 比如点击了 "开始转发" 并成功转发后, 这三个按钮中可操作的按钮就只有"停止转发", 点击"停止转发"后, 这三个按钮可操作的就是"开始转发"和"浏览"按钮, (顺便把这个"浏览"按钮的名称修改为"设置日志目录"), 然后中间那个"记录目录: "这个显示就直接去掉了后面留点缝隙直接就是输入框就行
7.数据源为文件时那三个限速的三个滑块的长度修改为向父级容器对齐, "追加写入"的开关向左和"限速"开关对齐, 中间留一些空间; 上方的"读文件->发送"和"接收->写入文件"这两个 radio 中间空隙稍微增加一些
8.数据源中的串口列表后面的"刷新"按钮以及数据源中"文件"的"浏览"按钮稍微拉宽一点, 汉字显示不全
9.在最下方的"事件日志"右边的"清空"按钮的上方方新增一个"导出"按钮, 可以将当前的内容输出为一个文件, 在"导出"按钮的上方增加一个"暂停"按钮, 点击一次变成"继续"再点一次变成"暂停"用于控制日志的暂停继续, 这三个按钮在竖方向均分分别和日志文本框一样高度
10.这个主题风格的直角/圆角能不能自动检测并设定默认值, 比如检测当前是 win10 系统就默认直角, 检测 win11 系统就默认圆角, 其他系统默认圆角, (如果检测操作系统和操作系统的版本不好实现就忽略本意见); 这是自动默认值的逻辑, 软件启动后切换的按钮仍然可用
11.主题风格的"黑色主题"和"白色主题"能不能根据时间自动默认值, 比如系统时间白天08:00~20:00自动为白色主题, 夜晚时间自动为黑色主题, 这是自动默认值的逻辑, 软件启动后切换按钮仍然可用


0.UI版本功能和CMD版本功能完全一致, 就是 UI 和 CMD 只是不同的使用接口, 内部的核心逻辑需要是单一统一的, python能打包为exe, 这个exe直接双击就是打开UI界面, 如果exe传入了其他命令行参数就是使用CMD, 这里可以使用类似于 --nogui 之类 (具体参数名称自行确定, 这里只是举例) 的参数控制, 如果传入了命令参数但是没有指定 --nogui, 那么仍然打开图形化, 并将已经指定的参数直接填充到图形化设置中, 比如我使用命令行参数指定好了其中一个数据源, 那么打开图形化的时候这个数据源就是配置好的, 如果运行的系统没有图形化环境无法启动UI则抛异常等提示错误
1.当数据源是文件 (读取文件向外发送) 时, 增加一个进度条(因为文件的体积一般是已知且固定的)
2.当数据源是文件 (读取文件向外发送) 时, 发送的速度控制滑块可以更加精细一点, 当前的速度挡位很少, 可以弄成多滑块, 单位为 B/KB/MB 共三个滑块, 每个滑块可以取值 0~1024, 总速度就取三个单位速度的叠加,比如 500B + 20KB + 0MB 意思就是控制发送速度上限为 20KB/s + 500B/s
3.当某个数据源是 TCP客户端 时, 允许用户设置本地 IP:PORT (当前只提供了设置服务器IP:PORT), 也就是增加一个接口可以让用户自己选择"让操作系统自动分配本地IP:PORT"/"手动设定本地IP:PORT"(可以自动扫描网卡IP)
4.网卡地址扫描增加一个 127.0.0.1
5.数据源是 TCP 服务端时, 增加一个接口让用户可以自己设置监听队列大小或者选择自动给定默认值
6.数据源 "UDP客户端"/"UDP服务端" 这里我之前描述不太准确, UDP无连接准确来说并无客户端/服务端之分, 所以这里把这两个数据源合并成一个 "UDP单播", 按照UDP的通信特点, 其一有可能是远程一个UDP程序会向当前本机某IP:PORT发送数据报, 这时候用户就应该自定义本地的IP:PORT(可以自动扫描网卡IP), 对端的就可以不设置, 等到收到一次远端主动发送的数据后, 这里的对端可以自动填充; 其二可能是我主动向一个远程IP:PORT发送数据, 对方收到一次后可以返回返回数据(这取决于对方, 对方愿意返回数据就返回, 对方如果只接收那我们也就是单纯只发送, 这在逻辑上是统一的), 这时候用户就应该自定义对端的IP:PORT, 而本地的IP:PORT可以选择自动分配或者手动填写; 虽然UDP可以接收/发送多个对端, 但是这里 "UDP单播" 我们就认为是点对点通信, 其中一方主动 (我方主动发送第一帧数据/或者对端主动发送第一帧数据), 随后理论上就可以双向通信了;
7.数据源是组播时增加一个按钮检测用户输入的地址是否是有效的组播地址, 输入框边上增加一个固定打印提示有效地址范围 224.0.0.0/4
8.数据源是组播时, 当前逻辑有bug, 比如数据源M设置为串口, 串口在源源不断输出数据, 此时数据源W设置为组播, 这时候串口数据会发送到组播, 但是同时它又收到了自己的刚刚发出的组播数据, 对于串口就像是接入了一个回声程序, 我希望设置组播时即向该组播发送数据, 又可以接收组播数据但是除了自己发送的以外, 类似于关闭 IP_MULTICAST_LOOP 之类的
9.数据源是广播时增加一个允许用户自定义广播范围的功能, 即让用户输入自己的广播地址, 程序自动检测网卡, 比如有两块网卡当前配置为 192.168.10.1/24 和 10.0.0.1/8 和 127.0.0.1/8 等等, 那么就可以自动下拉填充 192.168.10.255 或者 10.255.255.255, 127.255.255.255 等等, 或者用户可以自己输入, 总之用户可以自己控制广播范围, 对于发送广播时使用的本地端口允许自动分配或者用户手动定义
10.对于 "TCP服务端"/"组播"/"广播", 其对端可能有多个, 我希望增加一个对端列表的功能(但是UI上点击打开数据源后数据源的配置都变成灰色不可设置了, 所以这个列表可以放在"打开数据源"按钮的下方), 可以让用户选择一个"主要对端", 对于"TCP服务端"选择一个"主要对端"意味着和对端进行点对点通信, 如果不设置主要对端, TCP服务端会将自己的数据全部转发给所有对端, 并接收所有对端的数据; 对于"组播"/"广播"选择一个"主要对端"意味着"只接收它的返回数据, 其他的数据不接收", 如果不设置主要对端, 那么就所有对端全部接收
11.把UI和排版弄的更加现代化和美观一点, 尽可能的美观大气, (当前的UI看着有点像Win7时代的古董UI)
12.UI界面上显示的 "M->W: 0B (0 B/s) W->M: 0B (0 B/s)" 可以直接分别放到数据源M和W下方, 表示该数据源数据产生的速率, 当数据源是串口时额外显示一个百分比比如串口 115200-8-N-1 可以计算得知其理论速度上限, 再根据当前数据产生速率就可以计算一个占用百分比了
13.数据预览的"启用预览"从方框勾选改成滑动开关, 而且让其在开始转发后也能开关生效, (当前这个启用预留只有转发前设置才有效, 开始转发后切换开关状态就无效了), 然后这里的数据预留划分左右两个区域分别对应数据源M和数据源W, 各自显示各自的数据, 可显示 HEX 和 TXT, 可暂停/继续/清空, 两个区域各自可以控制数据的显示/不显示/HEX/TXT/暂停/继续/清空
14.当前的"记录转发数据到文件(HEX+ASCII)" 的功能具体细分一下, 可以支持分别可选择的单独存储数据源W和M的数据, 可选择按照二进制 BIN 存储或者ASCII TXT 存储, 用户只需要选择存储的 basedir, 如果用户开启了日志存储(哪怕数据源M/W只开启了一个日志存储), 程序自动新建一个 "2026-09-10-11:20:56" 的文件夹, 里面存的格式 "M.bin"/"M.txt"/"W.bin"/"W.txt"; 当用户将存储格式设置为二进制时, 只存储原始二进制数据不要自动加入时间戳等信息, 存储日志选择 "M.bin" 或者 "W.bin"; 当用户将日志存储设置为 TXT 时, 允许设置关闭时间戳或者开启并自定义每间隔 ... ms 存储一次时间戳,存储时间戳时要有自动换行
16.UI最下面是事件日志也就是本软件自身的操作记录, 增加一个清空按钮
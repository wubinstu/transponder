主要是一些细节优化:
0.添加 .gitignore 文件, 做好 git 版本管理, 后面每当提一次修改意见就形成一个版本
1.数据源作为文件发送时, 三个滑块可以稍微设置长一点, 当前滑块很短, 鼠标滑动一点数值波动较大, 另外统计处的限速单位始终是 B/s 这不符合人的阅读习惯, 用保留两位的浮点数, 单位智能更换为 B/KB/MB, "追加写入" 的勾选框改成开关; 数据发送过程看看是否允许继续滑动速度限制滑块(可以让用户动态调整"速度上限"); 还有这时候下面事件日志会一致输出比如 "M→W 转发失败: 文件(读)源不支持写入", 这个首先不属于失败, 我们设计就是文件作为发送就是不写入的, 然后这个提示做多提示一次就行, 不要一直提示, 所有相同的提示间隔几秒输出一次, 下面的事件日志每行带有统一宽度格式的时间戳
2.当前软件所有能触发小的消息窗口(比如检测地址是否可用之类的)都没有文字显示, 不要用这个弹窗, 用浮动的通知, 会自动消失, 而不是需要移动鼠标点击才能关闭
3.在当前黑色的主题下, 数据源的下拉列表"串口/文件/..."这个下拉列表会有一个上下的细长的白色边缘, 包括串口选择列表等, 但是并不是所有的下拉列表都有白边, 比如波特率下拉列表就是正常的没有白边, 好像是"纯下拉列表"就有这个白边, 而"可下拉可自定义输入的"下拉列表就没有这个白边
4."可下拉可自定义输入的"下拉列表鼠标点击输入框中间就是手动输入, 鼠标点击输入框末尾就触发了下拉列表, 这个逻辑没有问题, 就是末尾没有向下的图标显示, 这个优化一下
5.对端列表和设置主要对端的逻辑没有问题, 建议增加一个客户端自增序号, 按照连接顺序就行, 断开的客户端的序号不用收回, 序号可以不连续, 默认按照序号排序
6.增加一个白色主题, 有开关可以切换黑白主题, 增加一个任意的测试图标, 程序标题就叫做 MW数据转发器
7.一些类似于端口号的输入框右边有一个上下箭头, 点击可以增加/减少一个数值, 这个逻辑没有问题, 但是这个图标看不清(黑色主题下)
8.UDP单播测试没有逻辑问题, 建议给本地IP:PORT增加一个可用/占用检测
9.黑色主题下代开一个数据源后数据源的配置没有变色(比如之前白色主题是这些配置就变暗了), 不能很好的提示用户当前不可修改, 需要先关闭数据源
10.希望组播能添加可以自定义本地IP地址, 而不是默认 0.0.0.0, 这会自动选择网卡, 添加扫描和自定义网卡后用户可以自定义组播范围
11.当某些端口是自动选择的时候, 点击打开能自动填充显示
12.希望广播也能同时收发但是排除自己, 广播没有类似于 IP_MULTICAST_LOOP 这个功能, 那就试试能不能过滤自己的IP, 如果能过滤自己的IP实现"同时收发但是排除自己"那么组播也用这种方式(不再用IP_MULTICAST_LOOP, 因为你不是说Windows上会整机禁用吗? 这不方便测试)
13.M/W 数据预览做的很不错, 功能全部正确, 只是"暂停"/"清空"这两个按钮稍微宽一点, 这个汉字显示不下了
14.数据落地记录模块耦合在一起, M/W记录开关分开了, 但是记录格式,时间戳等共用了, 把这个也分开, 也可以把这个记录的功能直接拆分弄到数据预留中, 而共用的 basedir 这个可以放到 "开始转发"/"停止转发" 按钮后面, 这两个按钮后面还有长块的空闲地方
15.UI的排布布局整体优化优化一下
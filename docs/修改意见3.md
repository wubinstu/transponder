
0.当数据源的按钮显示为"打开"时, 把按钮弄成绿色的, 当数据源已经打开此时按钮显示为"关闭"时, 把按钮弄成红色的
1.当数据源为文件发送时, 如果启用了不限制速度, 那么就不允许调整速度, 调整速度的滑块再长一点(不如直接向数据源的父级模块对齐), 另外这个滑块后面只显示了一个0, 只有我拖动一下才会显示 B/KB/MB 单位, 修改为让单位默认显示; 另外"追加写入"开关可以移动到限速开关同一行,当文件为发送模式时限速开关常亮(激活状态, 可以开/关)而追加开关变灰, 当文件为接收模式时限速开关变灰而追加开关常亮
2.当数据源为TCP客户端时, 如果"本机IP:PORT自动分配"开关处于打开状态, 那么下方的本地地址和端口就变灰色不可操作了, 这个逻辑是正确的, 但是如果这时候我"打开/关闭"切换一次数据源状态, 虽然"本机IP:PORT自动分配"开关处于仍然处于打开状态, 但是此时下方的本地地址和端口就又变得可以操作了, 把这个bug修改一下
3.数据源为TCP服务端时, 检测绑定地址是否可用的那个按钮拉宽一点, 字数显示不下, 还有"组播"的地址检测按钮也是
4.端口号输入框末尾的上下箭头在黑白主题下显示正常, 但是如果我把鼠标放在箭头上, 这个箭头图片就会覆盖输入框的部分边框, 这个bug看看能不能修复一下
5.TCP服务端监听队列输入框显示为默认时当我打开数据源时希望能自动填充实际值, 另外这里如果手动输入了2然后删除不能变回默认了, 除非手动输入0又会变成汉字默认, 包括"UDP单播"的"对端端口"等输入框也有问题, 我希望用户删除了输入后就自动变为"自动/默认"等提示了
6.一些输入框中的"默认"/"自动分配"/"自动", 把这个变成淡一点的提示(也就是留空显示的提示)而不是看起来真是输入了"默认"这个汉字一样
7."纯下拉列表"圆角正常, 而"可下拉可自定义输入的"下拉列表它是圆角和直角都有显示, 把这个bug修复一下
8.主题的圆角/直角最好也弄成类似于"黑色/白色"主题一样可以控制显示的风格; 另外数据源M和W, 以及数据预留M和W这两个显示元素中间有一个分隔符,它紧贴着左右不好看, 尤其是在圆角模式下, 它向上向下突出了一部分不好看, 显示的元素/控件之间留一点缝隙
9."数据预览"中如果模式为 HEX 模式, 比如换行符 '\n' (0x0A) 等不要真的换行, 原封不动的显示 0A 即可, 就是 HEX 严格按照原始值HEX显示, 只有 TXT 模式才"翻译"
10."数据预览"中的日志记录当前的"记录"开关等在"开始转发"后仍然可以操作, 而且操作是无效的, 修改一下, 改成开始转发后这些设置不可修改变成灰色, 只有停止转发后才能修改配置, 另外每次点击"开始转发"都要在basedir中新建一个文件夹(当前好像已经是这样的), 不过我希望"停止转发"后立刻释放日志文件句柄, 不要再占用日志文件以及旧的日期时间文件夹
11.网络中的"对端列表"在我重新打开数据源后还会保留上一次的状态, 只有我手动点击一次"对端列表"才会刷新并显示"(暂无对端)", 我希望这个刷新在我打开数据源接口时就自动触发
12.这个对端列表出现的时候会向下挤压数据预览的空间, 我希望能改成向上对其, 从目前来看"TCP服务端"/"组播"/"广播"的"对端列表"的上方都有空白的空间
13.我发现数据源的数据速率计算只有在"开始转发"的时候才更新, "停止转发"后这个速率状态会冻结在"最后一帧", 修改一下, 停止转发后所有速率清零
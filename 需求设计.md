项目名称: 数据转发器 (Data Transponder)
项目背景: 用于调试, 嵌入式软件人员有时候需要将数据在串口/网口/文件等之间来回传输, 本程序设计为一个适配器, 一个桥梁, 让数据在不同的数据源之间转发

## 技术栈 (已确定)

- 语言: Python 3.10+
- GUI: PySide6 (Qt6)
- 串口: pyserial; 网卡信息: psutil
- 打包: PyInstaller (可打包为 Windows 单文件 exe; Linux 下可直接运行 python)
- 平台: Windows / Linux
- 界面与 CLI 帮助文本语言: 中文
- 同一时刻只支持一对 M↔W 转发; 需要多通道时多开几个程序实例即可

## UI 与 CLI 的关系

- UI 和 CMD 只是两个不同的使用接口, 内部核心逻辑单一统一, 功能完全一致
- exe 双击 (无参数) → 打开图形界面
- exe 带命令行参数但未指定 `--nogui` → 仍然打开图形界面, 并把已指定的参数直接填充到界面对应配置项 (如已用参数指定了某个数据源, 打开后该数据源即配置完成)
- exe 指定 `--nogui` → 进入纯命令行转发模式
- 查询类参数 (`--list-serial` / `--list-net`) 直接执行输出, 不打开界面
- 运行环境无图形化环境无法启动 UI 时, 抛出异常并给出明确错误提示

## 数据源类型

    - 普通文本/二进制文件
    - 串口
    - 网络 TCP 客户端
    - 网络 TCP 服务端
    - 网络 UDP 单播 (UDP 无连接, 不再区分客户端/服务端)
    - 网络组播
    - 网络广播

## 使用流程

    打开软件, 选择数据源 M, 并根据不同数据源配置必要参数, 然后打开; 再次选择数据源 W, 并根据不同数据源配置必要参数, 然后打开;
    两个数据源准备好后, 点击开始转发, 那么数据源 M 的数据就会转发给 W, W 的数据就会转发给 M, 直到点击停止转发
    停止转发后两个数据源保持打开, 可再次开始

## 数据源详细规则

### 文件
    - 文件只能单向转发: 从文件读取发送给另一端, 或从另一端接收写入文件; 不允许同时读写
    - 文件 ↔ 文件 = 单向复制/覆盖文件
    - 文件作为发送源: 单次读完即止 (读完自动停止该方向转发并提示, 另一方向不受影响)
    - 文件作为发送源时显示进度条 (已发送/总大小)
    - 发送限速由 B/KB/MB 三个滑块叠加控制, 每个滑块取值 0~1024, 总速率 = B + 1024×KB + 1024×1024×MB (字节/秒); 另有独立"不限速"开关 (默认开), 关闭时限速生效, 若三滑块全为 0 按 1 B/s 处理

### 网络
    - TCP 客户端: 主动连接远程服务端, 视为点对点转发; 支持设置本地 IP:PORT, 可选"操作系统自动分配"或手动指定 (IP 可从扫描的网卡列表选择)
    - TCP 服务端: 监听队列 (backlog) 大小可选"默认"或手动指定; 默认取第一个接入的客户端做点对点转发; 支持对端列表 (见下)
    - UDP 单播: 点对点。本地 IP:PORT 可自动分配或手动指定 (IP 从网卡列表选择); 对端可选填——
        · 未填对端: 收到第一帧远端数据后自动锁定该地址为对端
        · 已填对端: 发送发给该对端, 且仅接收该对端发来的数据报 (严格点对点)
    - 组播: 加入组播组收发; 输入框旁固定提示有效地址范围 224.0.0.0/4, 并提供"检测"按钮校验输入是否为有效组播地址; 关闭 IP_MULTICAST_LOOP, 自己发出的组播数据不回环到自己的接收端
    - 广播: 广播地址可从自动扫描的网卡广播地址下拉选择 (如 192.168.10.255 / 10.255.255.255 / 127.255.255.255) 或手动输入; 发送所用本地端口可自动分配或手动指定

### 对端列表 (TCP服务端 / 组播 / 广播)
    - 打开数据源后, 在"打开数据源"按钮下方显示对端列表
    - TCP服务端: 列出所有已接入客户端; 选择"主要对端"后仅与该客户端点对点通信 (其他客户端保持连接待选, 不参与转发); 不选主要对端时, 发送数据全部转发给所有对端, 并接收所有对端的数据
    - 组播/广播: 列表显示出现过数据报的来源地址; 选择"主要对端"表示只接收它的数据, 其他来源不接收; 不选则全部接收
    - 主要对端断开 (TCP) 后自动清空选择, 恢复"全部"模式并提示

### 串口
    - 参数: 端口/波特率/数据位/校验/停止位/流控
    - 自动扫描串口形成下拉列表, 并检测占用状态 (被占用的端口标注且不可选)

## 端口与地址检测

    - 自动扫描网卡 IP 形成下拉列表 (含 127.0.0.1)
    - 服务端 IP:PORT 能检测是否被占用
    - 客户端/UDP 本地端口由操作系统自动分配时, 在界面上显示实际分配结果

## 转发控制与状态

    - 点击停止转发后, 两个数据源保持打开状态, 可直接再次点击开始
    - 每个数据源面板下方实时显示该源的数据产生速率与累计流量; 数据源为串口时额外显示带宽占用百分比 (按波特率推算理论上限, 如 115200-8-N-1 ≈ 11520 B/s)
    - 数据预览: 分左右两个区域分别对应 M/W, 各自独立控制 显示开关(滑动开关)/HEX/TXT/暂停/继续/清空; 显示开关在转发进行中切换即时生效

## 数据落地 (旁路记录)

    - 可分别选择单独存储 M 或 W 的数据 (至少开启其一)
    - 格式可选 BIN 或 TXT:
        · BIN: 仅存储原始二进制数据, 不加任何时间戳等信息
        · TXT: 存储数据文本, 时间戳可关闭, 或开启并自定义间隔 (每 ... ms 输出一次), 输出时间戳时自动换行
    - 用户只需选择存储根目录 (basedir); 开启记录后程序自动创建时间戳文件夹, 命名格式 `2026-09-10_11-20-56` (Windows 文件名不允许冒号)
    - 文件命名为 `M.bin` / `M.txt` / `W.bin` / `W.txt`, 保存在该文件夹内

## UI 外观

    - 现代化、简约、美观大气 (现代深色主题、卡片式布局、滑动开关等控件)

## 其他要求

    - 合理利用继承多态组合设计模型, 代码清晰有必要注释
    - 串口/网络读写在线程中执行, 不阻塞 UI
    - UI 最下方为事件日志 (软件自身操作记录), 提供清空按钮

项目名称: MW数据转发器 (Data Transponder)
项目背景: 用于调试, 嵌入式软件人员有时候需要将数据在串口/网口/文件等之间来回传输, 本程序设计为一个适配器, 一个桥梁, 让数据在不同的数据源之间转发

## 技术栈 (已确定)

- 语言: Python 3.10+
- GUI: PySide6 (Qt6)
- 串口: pyserial; 网卡信息: psutil
- 打包: PyInstaller (可打包为 Windows 单文件 exe; Linux 下可直接运行 python)
- 平台: Windows / Linux
- 界面与 CLI 帮助文本语言: 中文
- 版本管理: git, 每一轮修改意见形成一个版本提交
- 同一时刻只支持一对 M↔W 转发; 需要多通道时多开几个程序实例即可

## UI 与 CLI 的关系

- UI 和 CMD 只是两个不同的使用接口, 内部核心逻辑单一统一, 功能完全一致
- exe 双击 (无参数) → 打开图形界面
- exe 带命令行参数但未指定 `--nogui` → 仍然打开图形界面, 并把已指定的参数直接填充到界面对应配置项
- exe 指定 `--nogui` → 进入纯命令行转发模式
- 查询类参数 (`--list-serial` / `--list-net`) 直接执行输出, 不打开界面
- 运行环境无图形化环境无法启动 UI 时, 抛出异常并给出明确错误提示

## 数据源类型

    - 普通文本/二进制文件
    - 串口
    - 网络 TCP 客户端
    - 网络 TCP 服务端
    - 网络 UDP 单播 (UDP 无连接, 不再区分客户端/服务端)
    - 网络组播
    - 网络广播

## 使用流程

    打开软件, 选择数据源 M, 并根据不同数据源配置必要参数, 然后打开; 再次选择数据源 W, 并根据不同数据源配置必要参数, 然后打开;
    两个数据源准备好后, 点击开始转发, 那么数据源 M 的数据就会转发给 W, W 的数据就会转发给 M, 直到点击停止转发
    停止转发后两个数据源保持打开, 可再次开始

## 数据源详细规则

### 文件
    - 文件只能单向转发: 从文件读取发送给另一端, 或从另一端接收写入文件; 不允许同时读写
    - 文件 ↔ 文件 = 单向复制/覆盖文件
    - 文件作为发送源: 单次读完即止, 显示进度条 (已发送/总大小)
    - 发送限速由 B/KB/MB 三个滑块叠加控制, 每个滑块取值 0~1024, 总速率 = B + 1024×KB + 1024×1024×MB (字节/秒); 另有独立"不限速"开关 (默认开), 关闭时限速生效, 若三滑块全为 0 按 1 B/s 处理; 限速显示用智能单位 (B/KB/MB, 保留两位小数)
    - "不限速"开关与"追加写入"开关位于同一行: 发送模式下限速开关常亮 (可开/关, 开启时滑块变灰不可调), 接收模式下限速整体变灰、追加开关常亮
    - 限速滑块长度与父级模块对齐, 单位标签默认显示 (0 B / 0 KB / 0 MB)
    - 限速滑块在数据源打开后、转发过程中均可动态调整, 即时生效

### 网络
    - TCP 客户端: 主动连接远程服务端, 视为点对点转发; 支持设置本地 IP:PORT, 可选"操作系统自动分配"或手动指定; 自动分配时打开后回显实际端口
    - TCP 服务端: 监听队列 (backlog) 大小可选"默认"或手动指定; 默认取第一个接入的客户端做点对点转发; 支持对端列表 (见下)
    - UDP 单播: 点对点。本地 IP:PORT 可自动分配或手动指定, 并提供绑定占用检测; 对端可选填——
        · 未填对端: 收到第一帧远端数据后自动锁定该地址为对端
        · 已填对端: 发送发给该对端, 且仅接收该对端发来的数据报 (严格点对点)
        · 自动分配端口时打开后回显实际端口
    - 组播: 加入组播组收发; 输入框旁固定提示有效地址范围 224.0.0.0/4, 并提供"检测"按钮; 本地接口(网卡)IP 可选 0.0.0.0(系统自动)或指定网卡; 自动过滤自己发出的组播数据 (按 来源IP=本机 且 来源端口=发送socket端口 识别), 本机其他组播程序不受影响, 可参与同机测试
    - 广播: 广播地址可从自动扫描的网卡广播地址下拉选择或手动输入; 发送所用本地端口可自动分配(打开后回显)或手动指定; 同样自动过滤自己发出的广播回环, 本机其他程序不受影响

### 对端列表 (TCP服务端 / 组播 / 广播)
    - 打开数据源后, 在数据源表单下方 (状态栏上方, 向上对齐) 显示对端列表; 打开时立即自动刷新, 不残留上一次内容
    - 每个对端带接入序号 (按连接顺序自增, 断开不回收), 默认按序号排序, 标注累计接收字节数
    - TCP服务端: 选择"主要对端"后仅与该客户端点对点通信 (其他客户端保持连接待选); 不选主要对端时, 发送数据全部转发给所有对端, 并接收所有对端的数据
    - 组播/广播: 选择"主要对端"表示只接收它的数据, 其他来源不接收; 不选则全部接收
    - 主要对端断开 (TCP) 后自动清空选择, 恢复"全部"模式并提示

### 串口
    - 参数: 端口/波特率/数据位/校验/停止位/流控
    - 自动扫描串口形成下拉列表, 并检测占用状态 (被占用的端口标注且不可选)

## 端口与地址检测

    - 自动扫描网卡 IP 形成下拉列表 (含 127.0.0.1); 服务端 IP:PORT 可检测 TCP 占用; UDP 本地绑定可检测占用
    - 各类自动分配的本地端口在打开数据源后自动回填实际值; TCP 服务端监听队列为"默认"时打开后回填实际值
    - 数字输入框的"默认/自动分配/自动"为淡色占位提示 (非真实输入的文字), 用户清空输入后自动回到占位状态

## 转发控制与状态

    - 点击停止转发后, 两个数据源保持打开状态, 可直接再次点击开始
    - 数据源"打开"按钮为绿色, 打开后显示"关闭"且为红色
    - 每个数据源面板下方实时显示该源的数据产生速率与累计流量; 数据源为串口时额外显示带宽占用百分比 (按波特率推算理论上限, 如 115200-8-N-1 ≈ 11520 B/s)
    - 停止转发后速率显示立即清零 (累计总量保留), 不冻结在最后一帧
    - 数据预览: 分左右两个区域分别对应 M/W, 各自独立控制 显示开关(滑动开关)/HEX/TXT/暂停/继续/清空; 显示开关在转发进行中切换即时生效
    - HEX 模式严格按原始字节显示 (如 0x0A 显示为 "0A", 不产生真实换行); 仅 TXT 模式做文本"翻译"
    - 落地记录配置 (分侧记录开关/格式/时间戳间隔/共用目录) 在转发开始后锁定变灰, 停止转发后恢复可改
    - 每次开始转发在 basedir 下新建时间戳文件夹; 停止转发后立即释放日志文件句柄, 不再占用日志文件与旧时间戳文件夹
    - 对端数据无法写入的预期场景 (如对端为文件只读源) 不算失败, 累计字节数低频提示; 相同文本的事件提示间隔数秒去重输出
    - 事件日志每行带统一宽度的时间戳 (HH:MM:SS.mmm)

## 数据落地 (旁路记录)

    - 记录配置分侧独立: M/W 各自的 记录开关 / 格式(BIN或TXT) / TXT时间戳间隔; 共用的存储根目录 basedir 放在开始/停止转发按钮旁
    - BIN: 仅存储原始二进制数据, 不加任何时间戳等信息
    - TXT: 存储数据文本, 时间戳可关闭, 或开启并自定义间隔 (每 ... ms 输出一次), 输出时间戳时自动换行
    - 用户只需选择 basedir; 开启记录后程序自动创建时间戳文件夹, 命名格式 `2026-09-10_11-20-56` (Windows 文件名不允许冒号)
    - 文件命名为 `M.bin` / `M.txt` / `W.bin` / `W.txt`, 保存在该文件夹内

## UI 外观

    - 现代化、简约、美观大气; 深色/浅色主题一键切换; 圆角/直角风格一键切换 (独立于黑白主题); 程序标题 "MW数据转发器", 带应用图标
    - 所有提示类消息使用自动消失的浮动通知 (Toast), 不使用需点击关闭的模态弹窗 (需要用户决策的确认框除外)
    - 打开数据源后配置区呈灰色不可编辑
    - 关闭数据源后表单恢复自身联动状态 (如 TCP客户端"自动分配"开关与本地地址/端口的联动)
    - 下拉列表 (含可编辑下拉) 带清晰的展开箭头且圆角一致; 数字输入框的上下调节箭头在两种主题下均清晰可见, 悬停时不覆盖输入框边框
    - M/W 面板与 M/W 预览区之间为透明分隔条, 控件间留有间隙, 圆角模式下不突出

## 其他要求

    - 合理利用继承多态组合设计模型, 代码清晰有必要注释
    - 串口/网络读写在线程中执行, 不阻塞 UI; 工作线程到 UI 的数据经线程安全队列泵送
    - UI 最下方为事件日志 (软件自身操作记录), 提供清空按钮

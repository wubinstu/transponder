0.把"切换为圆角"这个开关上的"切换为"三个字删除了, 就显示"圆角"/"直角"就行, 另外如果当前时直角, 那么就像是为"圆角", 如果当前时圆角, 那么就显示为"直角"
1.数据源为串口时, "波特率"/"数据位"/"校验"/"停止位"/"流控" 这些行其标题文字和输入框挤在一块了, 文字标签和输入框中间留一点空隙, 就像上方的串口列表这一行一样, 和它对齐
2.数据源为"UDP单播"中的"本地端口"和"对端端口"的输入框中的 hint 是淡淡的斜体字体, 那么把"对端地址"中的 hint 提示 "留空=收到第一帧数据后自动锁定对端"也弄成一模一样的样式, 也弄成斜体的
3."数据预览"模块中我希望当我点击"清空"按钮时, 下方的速率统计比如 "共15.9KB" 这个一并清空
4.当数据源为文件时, 我希望把"读文件->发送"和"接收->写文件"这两个 radio 和 "限速"/"追加写入"两个开关放在一起, 把文件路径浏览这一行放在前面, 下一行放置"读文件->发送"这个 ratio, 后面跟上限速开关; 下一行放置"接收->写文件" ratio, 后面跟上 "追加写入" 开关; 当然 radio 仍然是互斥的, 同时只能选中一个(只不过以前是放在同一行, 现在放在同一列); 并且选中"读文件->发送"这个 ratio 时 "追加写入" 开关要禁用, 反过来如果选中接收->写文件"这个 ratio 时"限速"开关要禁用; 然后下面三行就是三个限速用的滑块, 滑块长度对齐数据源模块右侧极限位置, 然后这个滑块能不能换个样式, 不太好操作感觉
5.最下方的"事件日志"里可以多显示一点内容, 比如我打开了数据源, 可以把数据源的配置信息也显示出来
6.数据源为"组播"时, "端口"和"TTL"这一行改成两个东西各占一半宽度